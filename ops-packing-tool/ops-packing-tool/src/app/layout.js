export const metadata = { title: 'Ops Bag Packing Tool', description: 'Bi-Rite Operations' };
export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
